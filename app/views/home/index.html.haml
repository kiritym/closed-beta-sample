%h2 Welcome to Closed Beta Sample App
- if !user_signed_in?
  = link_to "Be a part of Closed Beta Sample App", invitation_new_path, class: 'btn btn-info'
- else
  = link_to(destroy_user_session_path, :method => :delete, class: 'btn btn-danger') do
    Log out
